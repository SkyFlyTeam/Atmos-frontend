import axios from "axios";
import { Api } from "@/config/api";
import { Parametro } from "@/interfaces/Parametros";

const getAllParametros = async () => {
    return
};

const getParametroById = async (param_id: number) => {
    return
};

const createParametro = async (parametro: Parametro) => {
    return
};

const updateParametro = async (parametro: Parametro) => {
    return
};

const deleteParametro = async (param_id: number) => {
    return
};

export const parametroServices = {
    getAllParametros,
    getParametroById,
    createParametro,
    updateParametro,
    deleteParametro
};
  